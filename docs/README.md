# PayProof – docs

The in-app documentation lives at `/docs` (see `app/docs/page.tsx`). This directory is for supplementary markdown files as the project grows.

## What's here now

Nothing yet — the docs page is self-contained in the app. Markdown guides will be added here as the project matures.

## Planned additions

- `architecture.md` — walkthrough of `lib/stellar.ts` and the Horizon query flow
- `receipt-format.md` — explanation of the `{hash}-{operationIndex}` receipt ID scheme
- `contributing.md` — mirrors `CONTRIBUTING.md` with more code-level detail
