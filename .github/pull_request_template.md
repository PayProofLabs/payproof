## What does this PR do?

<!-- A short description. Link the related issue if there is one: Closes #123 -->

## How to test it

<!-- Steps to verify the change works. Include a testnet transaction hash if relevant. -->

## Checklist

- [ ] `npm run type-check` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Tested in browser (not just type-checked)
- [ ] If it touches the Horizon query logic, tested against a real testnet transaction
- [ ] If it changes the receipt layout, checked the print view (`Ctrl+P`)

## Screenshots (if UI changed)
